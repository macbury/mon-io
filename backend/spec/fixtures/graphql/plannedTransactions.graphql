query plannedTransactions($month: TimeArgument!) {
  plannedTransactions(month: $month) {
    amount {
      cents
    }
    date
    category {
      name
    }

    location {
      name
    }
  }
}