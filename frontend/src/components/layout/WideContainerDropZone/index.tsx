import React from 'react'
import WideContainer from '../WideContainer'
import { IDropZoneProps } from './types'

export default function WideContainerDropZone(props : IDropZoneProps) {
  return <WideContainer {...props} />
}